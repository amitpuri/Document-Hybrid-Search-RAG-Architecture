# ⚠️ DEPRECATED: Use `pip install -e .` with pyproject.toml extras instead
# This file is maintained for backwards compatibility only.
#
# For optional dependencies, use:
#   pip install -e .                    # Core only (PDF extraction + sparse search)
#   pip install -e .[retrieval]         # + Dense embeddings + Qdrant
#   pip install -e .[llm-all]           # + All LLM providers
#   pip install -e .[all]               # Everything
#
# See pyproject.toml for full dependency tree.

# ============================================================
# PDF Extraction Engines (Multi-backend support)
# ============================================================
pypdfium2>=4.0.0,<5.0.0
# Fallback PDF readers:
# pdfplumber>=0.10.0,<1.0.0
# pypdf>=3.0.0,<4.0.0

# ============================================================
# arXiv Ingestion (Phase 4)
# ============================================================
arxiv>=4.0.0,<5.0.0  # arXiv API client for automated corpus ingestion (note: v4.0+ uses urllib.request.urlretrieve instead of Result.download_pdf())

# ============================================================
# Core Numerics, Sparse Search & Vector Space Modeling
# ============================================================
numpy>=1.24.0,<2.0.0
scipy>=1.10.0,<2.0.0
scikit-learn>=1.2.0,<2.0.0
rank-bm25>=0.2.2,<1.0.0
pyarrow>=14.0.0,<15.0.0
networkx>=3.0,<4.0
qdrant-client>=1.11.0,<2.0.0          # Local & remote vector store and payload search

# ============================================================
# Neural Dense Embeddings & Cross-Encoder Re-Ranking
# ============================================================
sentence-transformers>=2.2.0,<3.0.0
torch>=2.0.0,<3.0.0
transformers>=4.35.0,<5.0.0   # required for SPECTER2 AutoModel/AutoTokenizer
adapters>=0.2.0,<1.0.0        # required for SPECTER2 proximity adapter loading

# ============================================================
# Environment & Configuration
# ============================================================
python-dotenv>=1.0.0,<2.0.0   # auto-loads .env into os.environ at startup

# ============================================================
# LLM Adapters — install the SDK for the provider you use
#
# Default models (configure via env or constructor arg):
#   OpenAI   -> gpt-5          (OPENAI_API_KEY)
#   Anthropic-> claude-sonnet-5 (ANTHROPIC_API_KEY)
#   Gemini   -> gemini-3.8-flash (GEMINI_API_KEY)
# ============================================================
openai>=1.0.0,<2.0.0                  # OpenAI: gpt-5, gpt-4o, o-series
google-genai>=1.0.0,<2.0.0            # Gemini: gemini-3.8-flash, gemini-2.5-pro (new SDK)
anthropic>=0.34.0,<1.0.0              # Anthropic: claude-sonnet-5, claude-opus-4

# ============================================================
# Cloud Platform SDKs for Multi-Route Deployment
# ============================================================
boto3>=1.28.0,<2.0.0                  # AWS Bedrock: Anthropic on Bedrock, region config
google-cloud-aiplatform>=1.38.0,<2.0.0 # GCP Vertex AI: Gemini on Vertex, project/location config
